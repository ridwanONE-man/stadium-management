name: myfirstproject
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  printing: ^5.9.0
  pdf: ^3.11.1
  firebase_core: ^2.10.0  # Add this dependency for Firebase initialization
  firebase_auth: ^4.5.0   # Firebase Authentication (optional)
  cloud_firestore: ^4.17.5 # Downgraded to avoid compatibility issues
  firebase_storage: ^11.1.0 # Firebase Storage (optional)

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/shop/
